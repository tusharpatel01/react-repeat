import React from "react";
const usercontext=React.createContext()
export default usercontext; 

//we also have to create a provider in which we wrap up all components and they have access of usercontext
